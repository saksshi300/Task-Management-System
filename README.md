# Task-Management-System
Task Management System is a full‑stack app with a Next.js frontend and Express + Prisma backend. It supports user authentication and task CRUD operations with status, priority, and due dates. Built on SQLite, it offers a clean UI and secure JWT‑based workflow
